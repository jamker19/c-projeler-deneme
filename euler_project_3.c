#include <stdio.h>
#include <stdint.h>

// Project Euler Problem 3:
// Find the largest prime factor of the number 600851475143.
// This template is minimal and ready for implementation.

int main(void)
{
    uint64_t target = 600851475143ULL;
    // TODO: compute the largest prime factor of target

    printf("Largest prime factor of %llu is: %llu\n", target, (unsigned long long)0);
    return 0;
}
